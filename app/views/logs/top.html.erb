<div class="container">
    <div class="position-relative">
        <h1>
            <div><label>参戦：</label><label><%= @userLogs.length %>試合</label></div>
        </h1>
        <div class="favo-list-area">
            <%# お気に入りロゴエリア start %>
            <ul class="favo-list">
                <% @logoArry.each do |logo| %>
                <li class="favo-logo-list">
                    <%= image_tag logo, class: 'favo-logo' %>
                </li>
                <% end %>
            </ul>
            <%# お気に入りロゴエリア end %>
        </div>
    </div>
    <div class="main">
        <% @allGames.each do |allGame| %>
        <section class="match-list-wrap" id="sectionGameId<%= allGame["game_id"] %>">
            <div class="game-date align-l">
                <h4 class="border-l-red">
                    <%# 曜日算出 %>
                    <% index = allGame["game_date"].strftime("%u").to_i %>
                    <%= allGame["game_date"].strftime("%Y年%m月%d日 (#{@weeks[index-1]})") %>
                </h4>
            </div>
            <table class="match-table align-l">
                <!-- ここをリンクにする start -->
                <tbody>
                    <tr>
                        <td class="match">
                            <table class="game-table">
                                <thead>
                                    <tr>
                                        <th>対戦カード</th>
                                        <th>スタジアム</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <tr>
                                        <td class="club-name home-side">
                                            <%= image_tag allGame["team_logo_url_home"], class: 'favo-logo' %>
                                            <span class="">
                                                <%= allGame["team_name_home"] %>
                                            </span>
                                        </td>
                                        <td class="point home-side">
                                            <%= allGame["score_home"] %>
                                        </td>
                                        <td class="vs">VS</td>
                                        <td class="point away-side">
                                            <%= allGame["score_away"] %>
                                        </td>
                                        <td class="club-name away-side">
                                            <%= image_tag allGame["team_logo_url_away"], class: 'favo-logo' %>
                                            <span class="">
                                                <%= allGame["team_name_away"] %>
                                            </span>
                                        </td>
                                        <td class="stadium">
                                            <%= allGame["stadiam_name"] %>
                                        </td>
                                    </tr>
                                </tbody>
                            </table>
                        </td>
                    </tr>
                </tbody>
                <!-- ここをリンクにする end -->
            </table>
            <input type="hidden" name="gameId_<%= allGame["game_id"] %>" value="<%= allGame["game_id"] %>">
        </section>
        <% end %>
    </div>
</div>
<div class="form">
    <%= button_to "select/topへ>>",{controller: "select", action: "top" }, {method: :get, class: "form-btn"} %>
</div>